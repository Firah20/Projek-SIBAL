// select-positioner.js — disabled to avoid interfering with Dash/React rendering.
// Previous positioner caused layout issues for your setup. Keeping as a no-op
// so the app uses the browser's native/react-select positioning.
(function(){
    // intentionally empty placeholder
})();
